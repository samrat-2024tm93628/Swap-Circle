version: '3.8'

services:
  api-gateway:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    # Allows container to reach other services exposed on your host machine
    extra_hosts:
      - "host.docker.internal:host-gateway"
